apiVersion: apps/v1
kind: Deployment
metadata:
  name: eva-seqcol
spec:
  replicas: 3  # 1 replica per node across the 3-node staging cluster
  template:
    spec:
      containers:
      - name: eva-seqcol
        image: dockerhub.ebi.ac.uk/ebivariation/eva-seqcol
        imagePullPolicy: Always
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
