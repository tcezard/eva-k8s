apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eva-seqcol-local

resources:
- namespace.yaml
- postgres.yaml
- ../../base

# Patch the base deployment for local testing
patches:
- path: deployment-patch.yaml
- path: service-patch.yaml
# Skip ingress and base secrets for local testing
- target:
    group: networking.k8s.io
    version: v1
    kind: Ingress
    name: eva-seqcol
  patch: |
    - op: replace
      path: /spec/rules/0/host
      value: localhost

# Use local image
images:
- name: ebivariation/eva-seqcol
  newName: eva-seqcol
  newTag: local

# Generate secret with application.properties for local testing
secretGenerator:
- name: eva-seqcol-config
  files:
  - application.properties

# Init script that creates the "eva" schema the app expects, run by the postgres
# image's /docker-entrypoint-initdb.d convention (see postgres.yaml)
configMapGenerator:
- name: postgres-initdb
  files:
  - init-eva-schema.sql
